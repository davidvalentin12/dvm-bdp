export class Step {
  constructor(
              description: string,
              timerTime?: number,
              timer?: boolean, //false pasive, true active
              title?: string
  ) {
  }
}
